name: CI Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # Update to the latest version

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3  # Update to the latest version
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: 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

      - name: Build Docker image
        run: sudo docker build -t ghcr.io/hsein1bourgi/practical-w:latest .

      - name: Push Docker image to GHCR
        run: sudo docker push ghcr.io/hsein1bourgi/practical-w:latest
